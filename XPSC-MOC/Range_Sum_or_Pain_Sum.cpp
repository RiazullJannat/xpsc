#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    ll N, Q;
    cin >> N >> Q;

    while (Q--)
    {
        ll L, R;
        cin >> L >> R;
    }

    return 0;
}